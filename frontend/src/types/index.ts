
// Course interface, stores what's in a course durr
export interface Course {
  _id: string;
  Code: string;
  Title: string;
}

// Professor interface, stores what's in a professor durr
export interface Professor {
  _id: string;
  First_Name: string;
  Last_Name: string;
}
